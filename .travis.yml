sudo: require
language: cpp
compiler:
  - gcc
branches:
  only:
  - master

matrix:
  include:
    - os: linux
      addons:
        apt:
          packages:
            - cmake
      env:
        - MATRIX_EVAL="CC=gcc && CXX=g++"
before_install:
  - sudo apt-get install libsfml-dev
script:
  - mkdir build && cd build && cmake .. && make
